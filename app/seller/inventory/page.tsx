import { InventoryClient } from "./InventoryClient";

export const metadata = {
  title: "Inventory — My Market",
};

export default function SellerInventoryPage() {
  return <InventoryClient />;
}
